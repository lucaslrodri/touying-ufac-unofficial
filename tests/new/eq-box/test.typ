/// Synopsis: `#eq-box` inside an equation (whole and partial padding, tall content, in a line of text, colors, numbering and label) and the `>` shorthand.
#import "../../common.typ": *
#show: setup
#include "body.typ"
