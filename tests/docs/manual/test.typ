/// Synopsis: compile-only test (no `ref/`): the manual must build, i.e. every chapter, every live example, the
/// reference generated from the doc-comments and the pictures of the slide examples (`sh docs/build.sh` renders them).
#include "/docs/manual.typ"
