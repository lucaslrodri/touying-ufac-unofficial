// Body of the `sections/cover` test: included by test.typ (Tytanic) and by /tests.typ (the catalogue).
#import "../../common.typ": *
#title-slide()

== First slide after the cover
The slide counter skips the cover (`freeze-slide-counter`): this is slide 1.
