// Body of the `sections/titles` test: included by test.typ (Tytanic) and by /tests.typ (the catalogue).
#import "../../common.typ": *
== Normal slide with a long title to test the header shrinking done by Touying's fit-to-width
Title shrunk with `utils.fit-to-width(grow: false)`.

== Title with $f(x) = a x^2 + b x + c$, `raw`, *bold* and #underline[underline]
=== Pill with #underline[yellow underline], *white bold* and `code`
Math, chip, bold and underline in the header (bold is blue, like the rest of the title; the underline is yellow in title and pill).
