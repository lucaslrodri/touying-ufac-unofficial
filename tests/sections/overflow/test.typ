/// Synopsis: `===` repeats on pages created by automatic overflow (no `---`), replacement by a new `===`, `---` continuation that overflows, and `#pause`.
#import "../../common.typ": *
#show: setup
#include "body.typ"
