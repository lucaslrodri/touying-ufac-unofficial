/// Synopsis: the theme's cover: list markers, `-> item`, `===` pill, `> ` box and `"quote"` veiled after `#pause`; no phantom subtitle pill on the 2nd subpage; a covered block that breaks across pages is hidden.
#import "../../common.typ": *
#show: setup
#include "body.typ"
